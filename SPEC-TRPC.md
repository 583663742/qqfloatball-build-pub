# QQFloatBall TRPC 直调方案 · 规格定稿（v1.0）

> 定稿日期：2026-08-19 · 决策人：agent 全权 · 用户确认方向
> 前置结论：WebView 路线已证伪（task-center 网页自跳 Kuikly，页面内 JS 跳转不可拦截），弃用。

## 一、核心思路
插件进程内用 NSURLSession 直调 QQ 等级 TRPC 接口（levelTask/Get + ExecAct），
完全不经过 QQ 的 WebView 容器 → 绕开 跳转/清空/死锁 全部战场。

## 二、技术链路（阶段顺序 = 实施顺序）
```
阶段1 三件套验证：uin + skey + p_skey + bkn 全拿到 → 日志实证
阶段2 任务列表：POST levelTask/Get {mode:42} → 打印完整响应（iOS 真实字段）
阶段3 领奖闭环：ExecAct 逐个执行 + UI 实时日志 + 重试/跳过逻辑
阶段4 清理：删除 WebView 拦截/看门狗/Kuikly 逻辑（方案A 全删，git 备份可回滚）
```

## 三、功能清单（排序 = 优先级）
1. 一键做任务（点球 → 弹窗 → 自动跑全部未完成任务）
2. 任务日志实时显示（正在做哪个/成功/失败，UI 弹窗内滚动）
3. 已领取跳过（status=3 不重复请求）
4. 失败重试（单任务 2 次，仍失败跳过继续，不卡死）
5. 任务间隔 3 秒（防风控）
6. 悬浮球本体 + 拖拽 + 菜单（原版保留）
7. 抓包功能保留（原版功能，不动）

## 四、交互设计
- 悬浮球：原样（显示"一键做任务"入口）
- 点"一键做任务" → 弹窗：
  ```
  ⚡ 等级任务自动执行中…
  ✅ 已获取登录态 (uin=xxxx)
  📋 任务列表: 10 个，未完成 4 个
  ▶ 正在执行: 每日登录 (+3分) ...
  ✅ 完成: 每日登录
  ⏭ 跳过: 分享好友 (已领取)
  ...
  全部完成，用时 45 秒
  ```
- 弹窗可关闭，任务后台继续（新日志追加到日志文件）

## 五、关键参数（以 iOS 实测为准，安卓档案仅参考）
| 参数 | 来源 | 状态 |
|---|---|---|
| uin | QQ 当前账号（进程内取） | 阶段1 探测 |
| skey | QQLoginPSKeyManager（keyType 待探） | 阶段1 探测 |
| p_skey | QQLoginPSKeyManager keyType=1（ti 域） | ✅ 已验证 |
| bkn | skey hash 算法 | 阶段1 探测 |
| levelTask/Get | ti.qq.com/qqlevel/trpc/levelTask/Get?bkn= POST {mode:42} | 阶段2 |
| ExecAct | ti.qq.com/qqlevel/tianxuan/trpc/access/ExecAct?bkn= | 阶段3 |

## 六、防错机制（保证不出错）
1. 每阶段改代码前 git commit 备份（当前 843c2ce 为回滚点）
2. 装机前校验：dylib 哈希/权限 755/root:wheel/大小一致性
3. 每阶段装机后拉日志实证，不空口报成功
4. 出错立即回滚上一版本（30 秒可恢复）
5. 账号红线：阶段1 探测 uin 后**立即比对主号/小号**，若是主号立即停止报告用户
6. 不做任何可能导致 QQ 掉线的操作（不动登录态/不调登出接口）

## 七、风险与兜底
- ExecAct 参数错位（历史 -5001）→ 阶段2 有完整响应打底，构造前先打印响应字段
- iOS 平台字段差异（ClientPlat/mode）→ 以 iOS 实测响应为准，安卓档案不直接抄
- 抓包工具缺失 → 走"响应推断 + 接口文档"路线（阶段2 响应即文档）
- 领奖接口仍失败 → 查透为止，不预设失败；真不可行时退回 WebView 手动提示

## 八、验收标准（全过才算完）
1. 点球一键 → 弹窗实时日志完整走完一轮
2. 全部未完成任务执行完毕，已领取跳过
3. 日志文件有完整执行记录（请求/响应/结果）
4. 无崩溃、无 ellekit 报错、无死锁
5. 重复点击不重复领奖（幂等）
